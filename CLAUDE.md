# Airis — Claude 向けオーケストレーション指示

このリポジトリは **デザインをルールに従ってコード化し、別リポジトリへ Push する** ためのものです。
必要なのは**このリポジトリと Claude Code だけ**。デザインの入力は次のいずれでも受け付けます:

- **Figma**（既存デザインのコード化）… **メインの使い方**
- **Claude Design**（claude.ai/design で作り込んだデザインのコード化。途中からの確認・作り込み移行にも使える）
- **ラフ画・参考画像**（手描きスケッチの写真、スクリーンショット等のローカル画像）
- **言葉での要望**（「こういう画面が欲しい」→ Claude がデザインを提案）
- 参考サイト（「このサイトのような」）… 補助的

> Figma がメインだが、**Figma 無しでも全フローが成立する**こと（Claude Code だけで完結）がこのリポジトリの要件。

**このファイルはフロー全体（何を、どの順で、誰に確認するか）の「正」です。**
コードの書き方・ディレクトリ構成・テストの実装詳細は `rules/` が正で、ここでは繰り返しません。

---

## 前提

- デザイナーは非エンジニアの可能性があります。**専門用語を避け、日本語で、1 ステップずつ**案内してください。
- 破壊的・外部影響のある操作（別リポジトリへの `git push` など）は**必ず事前確認**を取ります。
- **生成物は Push 先リポジトリの作業ツリーへ直接書きます**（`output/` に書いてから配置し直す二段構えはしない）。`output/` は HTML モック（`preview/`）・診断の控え（`diagnostics/`）・クローン（`.push/`）だけに使います。
- **デザインシステムの層構造を尊重する**（Spotify Encore 参考）: トークンの決定と実装は**別の工程**であり、両者の答え合わせは Storybook 上で行う。トークンの追加・共通層への昇格・VRT 対象の追加は人間のプロセスであり、Claude は勝手に行わず候補の報告にとどめる（`rules/common.md` §2・`rules/web-app.md` §3.1.1）。

### 絶対に守る 4 つの原則

このフローで**繰り返し問題になった点**なので、例外を作らないこと。

1. **GitHub のブラウザ画面での手動操作を案内しない。**
  デザイナーはターミナルを使わないが、**Claude Code 経由で `git` / `gh` を使ってコミット・Push・PR 作成を行う**。
  「GitHub の画面で Add file → Upload files」のような案内は**しない**（トークンも生成コードも同じ）。
  例外は Claude が技術的に実行できない設定作業のみ（Pages の有効化・`gh auth login`）。
2. **Push 前にローカルでテスト・ビルド・Storybook 起動をしない。**
  検査は GitHub Actions が、見た目の確認は公開された Storybook が担う。
  「検証済み」と言うためにローカルで回すのは**二重で不要**であり、フロー全体を遅くする。
  例外は 4 つ。いずれもビルドでもテスト実行でもないので、Push 前に実行してよい:
  ① デザイナーが明示的にローカル確認を求めた場合
  ② **設定・スケール・色・命名の読み取り**（`style-dictionary build` / `scripts/effective-scale.mjs` / `scripts/contrast.mjs` / `scripts/lint-naming.mjs`）
  ③ **生成物のセルフチェック**（`scripts/selfcheck.mjs`。静的検査 + `tsc --noEmit`。数秒で終わる。ステップ 10）
  ④ **shadcn/ui のファイル取得**（`npx shadcn@latest add <部品>`。`rules/web-app.md` の生成直前チェックリスト 1）
3. **デザインソース側の不備を勝手に埋めない。**
  Figma の設定が不足・誤っていることは日常的にある。**推測で補完せず、「何が問題か」「デザインソース側でどう直すか」を提示して操作者に判断させる**（ステップ 7）。
4. **トークンにない値をコードに書かない。**
  ステップ 6 の「値 → トークン名」逆引き表に当たらない値は、arbitrary value（`bg-[#1a73e8]`）で埋めず**診断に回す**。

---

## フロー全体

```
1. デザインソースの確定（Figma / Claude Design / ラフ画 / 言葉 / 参考サイト）
2. デザインの確定
   A. Figma（メイン）  → MCP 接続 → 対象特定 → デザイン情報の取得
   B. 言葉・ラフ画     → ヒアリング → デザイン提案（プレビュー）→ 合意まで反復
   C. Claude Design   → 作り込み（起点/途中移行）→ デザイン一式の受け取り
3. プラットフォーム / ターゲット判定（Web アプリ / LP / コンテンツサイト / ネイティブ）
4. 変換ルールの読み込み
5. Push 先の確定 → 作業ツリーの用意（クローン。6〜8 がこの上で動く）
6. デザイントークンの確定（+ 「値 → トークン名」逆引き表の作成）
7. ★ 変換前診断 … デザインソース側の問題 + アーキテクチャ非適合を提示し、操作者に A/B/C を選ばせる
8. コード生成（作業ツリーへ直接）
9. Storybook / テスト成果物の生成
10. セルフチェック（静的検査 + 型検査）→ デザイナーへ確認
11. コミット → Push → PR 作成
```

> セットアップ（前提確認・作業ディレクトリ準備・任意の Figma 接続）は別コマンド `/setup` が担います。
> このフローの入口は `/design-to-code` です。

### 1. デザインソースの確定

まず「今日は何をもとに作りますか？」を確認します。引数や文脈から自明なら聞き直さない:

- Figma URL が渡された / 「Figma の〜」と言われた → **2-A: Figma ルート**
- 「Claude Design で作った」/ デザイン一式を渡された / claude.ai/design の話をされた → **2-C: Claude Design ルート**
- 画像が渡された / 「このラフから」 → **2-B: 言葉・ラフ画ルート**（画像を読み取って進める）
- 要望だけ → **2-B: 言葉・ラフ画ルート**
- 参考サイト URL → 2-B に準じ、見た目の参考として扱う（コードは複製しない）

> ルートは固定ではない。**A/B で進めている途中でも、「Claude Design で確認したい / 作り込みたい」と言われたら 2-C の該当パターンに切り替えてよい**。

### 2. デザインの確定（ルートは A / B / C）

デザインソースに応じて 2-A / 2-B / 2-C のいずれかを実施する。**どのルートでも「確定デザインを得る」までがこのステップ**。

#### 2-A. Figma ルート（メイン）

既存の Figma デザインをコード化するメインルート。2 つの Figma MCP に両対応し、**利用可能な方を自動で選ぶ**。

| MCP | サーバー名 | 特徴 | 対象の指定方法 |
| --- | --- | --- | --- |
| 公式 Dev Mode | `figma-dev-mode` | デスクトップアプリ内で起動。構造化情報が豊富。**Variables を読めるのはこちらだけ** | アプリ上で**選択中**のフレーム、または node-id |
| Framelink | `figma-framelink` | API トークンで動作。アプリ不要 | Figma **URL**（file key + node-id を含む） |

- `figma-dev-mode` のツール（`get_code` / `get_image` / `get_variable_defs` 等）が使えるなら **Dev Mode 優先**。使えなければ `figma-framelink`（`get_figma_data` / `download_figma_images` 等）。
- **Variables（変数）を MCP から読めるのは Dev Mode だけ**（Framelink は REST API 経由で、Variables REST API は Enterprise org の Full seat 必須）。`claude` モードでトークンを起こす場合はこの差が効くので、ステップ 6 の注意を参照する。
- どちらも使えない場合の案内:
  - Dev Mode: Figma デスクトップアプリで対象を開き、環境設定で「Enable local MCP Server」を有効化。
  - Framelink: `/setup` で `FIGMA_API_KEY` を設定（設定後は Claude Code を再起動）。

対象の特定と取得:

- **Dev Mode**: 「コード化したいフレームを Figma アプリで選択してください」と伝え、選択中の要素を対象にする。
- **Framelink**: 「対象の Figma URL を貼ってください」と依頼し、file key と `node-id` を抽出する。
- レイアウト構造・テキスト・色・サイズ・Auto Layout・コンポーネント・**Variables（トークン）**を取得する。
- **アセット（画像・アイコン）はここでは書き出さない**（作業ツリーはステップ 5 でしか用意されない）。必要なアセットの一覧だけ把握し、**実体の取得と配置はステップ 8** で行う（`rules/common.md` §6）。
- 複数フレームの一括変換は、対象リストを確認してから順に処理する。

> **`get_code` の出力をそのままコードにしない。** `get_code` は生の HEX や `bg-[#1a73e8]` を返すため、そのまま採用すると**トークンを経由しないコード**になる。`get_code` は**構造・階層・要素の役割を読み取るための資料**として扱い、値は必ずステップ 6 の逆引き表を通す。

#### 2-B. 言葉・ラフ画ルート

Figma を使わない場合のルート。**コード化の前に、デザインそのものを合意する**。

1. **ヒアリング**: 目的・想定ユーザー・必要な要素（入力欄、ボタン、一覧…）・トーン（かっちり/カジュアル等）を簡潔に聞く。ラフ画があれば読み取り、不明点だけ質問する。
2. **デザイン提案**: 依存なしで開ける**単一 HTML のモック**を `output/preview/<画面名>.html` に作り、ブラウザで開いて確認してもらう（`open` コマンドで開いてよい）。この段階のモックは使い捨てであり、`rules/` のコード規約には縛られない（見た目の合意が目的）。
3. **反復**: フィードバックを受けてモックを修正。**デザイナーが「これで OK」と言うまで**コード化に進まない。
4. 合意したモックを「確定デザイン」として扱い、ステップ 3 以降に進む。

#### 2-C. Claude Design ルート

**Claude Design**（claude.ai/design。研究プレビュー、Pro / Max / Team / Enterprise プランで利用可）で作られたデザインを扱うルート。Claude Code とは公式連携があり、**`/design` コマンド**（ターミナル内でのデザイン作成・編集・同期）と **デザイン一式の受け渡し**（公式機能名: design-to-code handoff）が使える。

使い方は 3 パターン:

1. **Claude Design 起点**（最初から Claude Design で作り込む）
  - デザイナーが Claude Design でプロジェクトを作成し、デザインを作り込む。完成したらデザイン一式を受け取る。
  - **Push 先に既存の生成物・トークンがある場合**は、作り始める前に Claude Design の **codebase-aware import** で既存デザインシステム（`tokens/`・`components/`）を読み込ませるよう案内する（デザインとコードの整合が最初から取れる）。
2. **途中から確認に使う**（Figma / ラフ画ルートの確認手段）
  - 2-A / 2-B で作成中のモックや生成物を Claude Design にアップロードし、デザイナーが視覚的に確認・微調整する。
  - コードはローカルで生成済み（または生成中）なので、確認 OK ならそのままステップ 10 → 11 へ進む。
3. **途中から作り込みに移行**（2-A / 2-B → Claude Design へ主体を移す）
  - それまでの確定内容（モック・取得済みデザイン情報・トークン）を引き継いで作り込みを続け、完成後はパターン 1 と同じくデザイン一式を受け取る。

**重要な原則**: 受け渡されたデザイン一式に含まれる生成コードは**そのまま採用しない**。デザイン一式は「確定デザイン」（構造・スタイル・意図の情報源）として扱い、**本生成物はステップ 4 以降で `rules/` 準拠で生成し直す**（トークン参照・命名規則・Storybook / テスト成果物を保証するため。2-B のモックと同じ位置づけ）。

- Claude Design が使えない場合（プラン外・プレビュー終了等）は、その旨を正直に伝えて 2-A / 2-B で代替する。

### 3. プラットフォーム / ターゲット判定

決めることは **2 段階**（Web か ネイティブか → Web ならどのターゲットか）。
**この 2 つを 1 つの質問に混ぜない。** 混ぜると「Web = ログイン後の画面」のような誤った説明になり、**LP（Astro）/ コンテンツサイト（Next.js）が選択肢として現れなくなる**。

> **フレームワーク選定は後戻りしにくい決定**なので、**黙って決めない**。自動検知や `config` の固定値で答えが出ている場合も、**「〜と判断しました。理由は〜。変更しますか？」と 1 行で伝えて確認を取る**（聞き直すのではなく、決定を見せて否認の機会を作る）。

#### 3-1. Web か ネイティブか

説明はこの言葉で（**SPA / ログイン後の画面といった下位の話を混ぜない**）:

| | どういうものか | 例 |
| --- | --- | --- |
| **Web** | ブラウザで見るもの。URL を開けば使える | 社内の管理画面、サービスサイト、LP、ブログ |
| **ネイティブアプリ** | App Store / Google Play から端末に入れて使うもの | スマホアプリ、タブレット専用アプリ |

- **Figma / Claude Design ルート**: フレームサイズ等から自動検知を試みる（モバイル端末サイズ・ノッチ/タブバー → ネイティブの手がかり。デスクトップ幅・レスポンシブグリッド → Web の手がかり）。
- **言葉・ラフ画ルート**: 要望から判断する。
- **ネイティブの可能性が高い、または判断がつかない → 必ずデザイナーに確認**。ネイティブなら**フレームワークも確認**（React Native / Flutter / SwiftUI / Kotlin。既定値なし、必ず聞く）。

#### 3-2. Web の 3 ターゲット

**判断がつかないときは、この表をそのまま見せて選んでもらう**（3 つの存在を隠さない）:

| | どんなページ向きか | 具体例 | 使う技術（`webTarget` の値） |
| --- | --- | --- | --- |
| **Web アプリ** | **検索から人は来ない**。ログインして使う道具 | 管理画面、社内ツール、ダッシュボード | React + Vite（`app`） |
| **LP** | **検索・SNS から人が来る**。1 枚で完結し、後からページは増えない | キャンペーンページ、製品紹介 1 枚 | Astro（`lp`） |
| **コンテンツサイト** | **検索・SNS から人が来る**。ページや記事が**増えていく** | ブログ、お知らせ、サービスサイト | Next.js（`content-site`） |

判定は次の 2 問で決まる（専門用語を避けた聞き方）:

1. 「この画面は **Google 検索や SNS から人が来る**ページですか？」
   - **来ない** → **Web アプリ**（`rules/web-app.md`）で確定。2 問目は不要
   - **来る** → 2 問目へ
2. 「**今後ページや記事が増えていく**予定はありますか？（お知らせ・ブログ・実績など）」
   - **増えない**（1 枚で完結） → **LP**（`rules/web-lp.md`）
   - **増える / 分からない** → **コンテンツサイト**（`rules/web-content-site.md`）

- `config/project.local.json` の `platform.default` / `platform.webTarget` が固定されていればそれを優先する。**ただし何を使ったかは必ず伝える**（例:「設定で `content-site` に固定されているので Next.js で作ります」）。
- **迷ったら Next.js 側に倒す。** Astro → Next は作り直しになるが、Next で作った 1 枚ページをそのまま運用し続けるのは問題にならない。
- Astro で作った LP にコンテンツ追加・動的配信が生えてきたら、Next への移行を提案する。

### 4. 変換ルールの読み込み

コード生成の**前に**必ず該当ルールを読み込む。

- 共通: `rules/common.md`（命名・トークン・余白・アクセシビリティ・アセット方針・診断）
- Web アプリ（SPA）: `rules/web-app.md`（**入口**。工程別に 5 ファイルへ分かれているので、冒頭の索引から**必要な工程のファイルだけを読む**）
  - スタイルを当てる → `rules/web-app-styling.md` / ストーリー → `rules/web-app-storybook.md` / テスト → `rules/web-app-testing.md` / CI → `rules/web-app-ci.md` / Push 前の検査 → `rules/web-app-selfcheck.md`
- LP: `rules/web-lp.md`（Astro。骨子版 — 不足があれば止まって確認）
- コンテンツサイト: `rules/web-content-site.md`（Next.js。骨子版 — 同上）
- ネイティブ: `rules/native.md`（選ばれたフレームワークの節に従う）

`rules/` はプロジェクト側で管理・拡張する領域。ルールとデザインが矛盾する場合は**ルールを優先**し、矛盾点をデザイナーに一言伝える。

### 5. Push 先の確定 → 作業ツリーの用意

**トークンの変換（6）と診断（7）とコード生成（8）はすべて Push 先の作業ツリー上で行う**（`config/sd.config.js` の実行、`node_modules/tailwindcss` からの実効スケール実測）。
そのため**クローンはここで済ませる。** 後回しにすると 6・7 が実行できない。

**Push 先は毎回プロンプトで確認する。** `config` に値があっても既定では使わない。

1. **Push 先リポジトリの URL**（例: `git@github.com:org/repo.git`）
2. **ブランチ名**（新規ブランチ。例: `design/<画面名>`）と、**PR のマージ先**（既定は `git.developBranch` = 開発ブランチ。develop が無い運用なら `git.mainBranch`）
   - **ブランチ保護は「取り決め」と「実際の設定」を区別する。** 運用ルールを聞いてそのまま生成物に書くと、**設定されていないのに「保護されている」と書く**ことになる。これは Pages の閲覧範囲（ステップ 9-1 の 2）とまったく同じ構図で、**安全だと誤認させる方向の誤り**なので同じ規律で扱う — **推測せず API で確認する**:

     ```bash
     gh api repos/<owner>/<repo>/branches/<マージ先>/protection   # classic 保護（404 = 未設定）
     gh api repos/<owner>/<repo>/rulesets                          # ruleset（[] = repo レベルには無い）
     ```

   - **classic 保護と ruleset は別機構なので両方見る**（片方だけでは判定できない）。
   - **`rulesets` が `[]` でも「保護なし」と断定しない。** org レベルの ruleset は `admin:org` スコープが無いと読めないため、言えるのは**「repo レベルには無い」**まで。
   - 生成物と PR 本文には**強制されているかどうかを書き分ける**（「PR 経由のみ」と書くのは実際に強制されている場合だけ。取り決めだけなら「運用上の取り決め（未強制）」と書く）。
   - 未設定なら、設定方法（**Settings → Branches → ruleset**）と **Claude では設定できない**ことを PR 本文に添える。
3. **配置先ディレクトリ**（リポジトリ内のパス。例: `src/`）
   - ただし **`tokens/` / `config/` / `tests/` / `.github/` / Playwright 設定は、配置先に関わらず必ずリポジトリのルート直下**（CI が root 相対で参照するため）。ここで聞いているのはコンポーネント・画面の置き場所だけ。詳細は `rules/web-app.md` §2。
   - **後で移し替えない**（ワークフローの相対パスがずれる）。

確認できたら作業ツリーを用意する:

- Push 先を `output/.push/<repo>` にクローン（既にあれば `git pull`）。認証は `gh auth status` で確認できる。
- 指定ブランチを作成/チェックアウト（`git switch -c <branch>`）。
- **以降の生成物はすべてこの作業ツリーに直接書く。**
- **`node_modules/` が無ければ `npm ci` の実行可否を確認する。** 無いと `style-dictionary`（6）と実効スケールの実測（7）ができず、診断が「未実施」に劣化する。

### 6. デザイントークンの確定（正本 = DTCG JSON、変換 = Style Dictionary）

コード生成の前に、色・余白・角丸・タイポの「単語帳」を確定する。
**構造・命名・逆引き表の書式は `rules/common.md` §2 が正**（Core 最小 + `product/<プロダクト名>/mode/`、適用順は `core` → `mode/default` → `mode/<モード>`・逆流禁止）。
ここではフロー上の判断だけを定める。

- **トークンの決定・保守は Claude の役割ではない。** 値もモードも勝手に増やさず、提案時は「Core とプロダクトのどちらに足すべきか」を添える。プロダクト名は `config` の `tokens.product`（未設定なら確認する）。
- **Claude が手で書き写さない。** 各ターゲット向けの出力（Tailwind theme / ネイティブテーマ）は **Style Dictionary で機械的に変換**する（同じ JSON からは必ず同じ出力が出ることを優先）。

#### 6-1. Figma ルート — 取り出し方を 4 つから選ぶ

`config/project.local.json` の `tokens.figmaExport`。**どれも対等な選択肢**であり、既定の「推奨」は置かない。未設定 / `ask` の場合はその場で選んでもらい、`project.local.json` に保存する（`/setup` の再実行でも変更できる）。

| 値 | 方法 | 向いている場面 | Claude の作業 |
| --- | --- | --- | --- |
| `tokens-studio` | **Tokens Studio プラグイン + GitHub Sync** | 継続的にデザインを更新する。プラグイン導入・GitHub トークン発行が許容できる | 事前に「Push 済みか」を確認し、同期先（通常は Push 先）から最新の `core/` と `product/<名前>/mode/` を取り込む |
| `custom-plugin` | **自作 Figma プラグイン**の書き出し JSON を受け取る | 社内にプラグインがある。独自の命名・構造規約がある | **初回に出力仕様を確認**（DTCG 準拠か / トークンセットと `core`・`mode/` の対応 / 参照の書き方）。DTCG でなければ正規化ルールを決めて `tokens.customPluginSpec` に記録し、以後同じ変換を再現する。仕様が読み取れなければ推測せずサンプルを見せてもらう。**取り込みルールが既にあるプラグインは下記** |
| `plugin` | **既製プラグイン**（例: TokensBrücke）の書き出し JSON を受け取る | 単発・小規模。導入は最小限にしたい | DTCG 形式でなければ DTCG に正規化する |
| `claude` | **プラグインを使わず Claude Code が変換**（MCP から取得して DTCG JSON を起こす） | プラグインを入れられない。まず試したい | **解釈が入る**ので、生成後に「名前 = 値 + 用途」の一覧を提示して**デザイナーの合意を取ってから**確定する |

> **`claude` を選ぶ前にプラン制約を伝える**: **Variables を読めるのは Dev Mode MCP（`get_variable_defs`）だけ**（Framelink の REST API 経由は Enterprise org の Full seat が必要）。
> Framelink しか無い場合、`claude` で拾えるのは Styles と実際に使われている値だけ。**その旨を伝えて**「値から名前を起こして合意する」進め方にする（言葉・ラフ画ルートに近い扱い）。
> **プラグイン方式は Plugin API を使うのでプラン制約を受けない** — Variables を確実に取りたいならそちらを案内する。

**取り込みルールが用意されているプラグイン**（`custom-plugin` を選ぶ場合の候補。**押しつけない**）:

| プラグイン | 取り込みルール | 触れ方 |
| --- | --- | --- |
| **Airis Design Tokens Export**（**このリポジトリに同梱**。`figma-plugin/`） | `rules/figma-plugin-airis.md` | **選択肢として一度だけ触れる**。「使えるかもしれません」程度にとどめ、要件に合うかの判断は操作者に委ねる |

> 触れ方の例（**これ以上推さない**）: 「Airis には **Airis Design Tokens Export** という Figma プラグインが同梱されていて、取り込みルールも用意してあります（Variables を DTCG で書き出せて、Enterprise プランが不要です）。`custom-plugin` を選ぶ場合の候補になりますが、要件に合うかは中身を見て判断してください。合わなければ他の 3 つでも問題ありません。」
>
> - **既に別のプラグインを使っている / 他の方式を選んだ場合は、それ以上言わない。** 乗り換えを勧めない。
> - 選ばれたら `tokens.figmaExport` を `custom-plugin`、`tokens.customPluginSpec` を `rules/figma-plugin-airis.md` に設定する。
> - 選ばれなかったら以後この提案を繰り返さない（同じセッションでも別セッションでも）。

**プラグインで書き出した JSON は「値の唯一の供給源」として使い切る**（受け取っただけで終わらせない）。用途は 2 つあり、**両方**を必ず行う:

1. 正本 `tokens/` に取り込む → Style Dictionary が `styles/tokens.css` を生成する（スタイルの実体）
2. **「値 → トークン名」の逆引き表を作ってチャットに提示する** → ステップ 8 のコード生成で、クラス名を決める唯一の参照元にする（書式は `rules/common.md` §2）

#### 6-2. Figma 以外のルート

- **言葉・ラフ画**: 確定デザイン（モック）から **Claude がトークンを起こして提案し、デザイナーと合意**した上で DTCG JSON にする。提案は「名前 = 値 + 用途」の一覧で簡潔に示す（例: `primary = #2563EB ボタン・リンク`）。ブランドカラー等の指定があれば最優先で反映する。
- **Claude Design**: 受け取ったデザイン一式のスタイル情報から起こす（言葉・ラフ画と同じ扱い）。codebase-aware import で既存デザインシステムを読み込ませて作った場合は既存トークンと整合しているはずなので、diff を確認して**差分だけ**を提案する。

#### 6-3. 変換の実行とコミット方針

- リポジトリの `style-dictionary`（`npm install` 済み。無ければ案内）を、生成した設定 `config/sd.config.js` と共に実行する。出力先・形式は各ターゲットのルールが正。
  - ローカルで実行するのは **`styles/tokens.css` を生成物として PR に含めるため**であり、テストの代わりではない（原則 2）。
- **トークンも生成コードと同じブランチ・同じ PR に入れ、コミット・Push は Claude が行う**（ステップ 11）。「トークンを先に反映」といった順序の段取りは不要。デザイナーにブラウザでのアップロードを頼まない。
  - 例外は `tokens-studio` のみ。この方式では**プラグインの Push ボタンがコミットと PR を作る**ので、Claude 側の作業は同期先からの取り込みだけになる（デザイナーの操作は Figma プラグイン内で完結し、GitHub の画面は触らない）。
- 同じ Push 先に過去の生成物がある場合は、**既存トークンとの整合**を確認し、無断で別の値を増やさない（正本 JSON 同士の diff で機械的に確認できる）。

### 7. 変換前診断

**コード生成に入る前に必ず実施する。** デザインソースの設定不足・誤りは日常的にあり、**推測で補完すると間違いがコードに焼き込まれたまま誰も気付けなくなる**。
「何が問題か」「デザインソース側でどう直すか」を洗い出して**操作者に返し、判断させる**（原則 3）。

> **診断を前倒ししない。** 「この余白 13px は Tailwind の余白スケールに乗らない」「このドロップダウンは shadcn/ui のどの部品にも当たらない」といった**アーキテクチャ非適合は、ターゲット（3）・ルール（4）・トークン（6）が確定していなければ判定できない**。
> 前倒しするとデザインソースの不備（命名・Auto Layout）しか拾えず、残りは実装時に arbitrary value や独自実装として静かに埋められる。

#### 7-1. まず対象部品を 1 つずつ下見する（**コードを 1 行も書かない**）

**機械判定だけでは足りない。** `lint-naming` / `contrast.mjs` / `effective-scale.mjs` と `components.json` の集計で拾えるのは命名・コントラスト・数値スケールまでで、**Figma 側への指摘の半分近くは 1 部品ずつ精読して初めて出る**。下見を省くと、それらが**生成中に出てきて**操作者が何度も判断を求められ、Figma の修正も小分けになる（A/B/C を選ばせた意味が無くなる）。

対象部品を 1 つずつ見て、次を取る。**この時点では報告しない**（7-3 で機械判定と合流させる）:

- Figma の事実 — バリアント軸と値・実寸・`layout` の `mode` / `pad` / `gap` / `r` / `sw`・`bindings`
- 既存コードがあれば読み、**差分と過去の判断**（`TODO`・理由コメント）を拾う

集計では出ず、下見でしか出ない種類:

| 種類 | 例 |
| --- | --- |
| バリアント同士の構造比較 | ページ送りの「最後」が「最初」と同一構成で、戻る導線が無い |
| 部品をまたいだ同一要素の突き合わせ | 評価の星が、部品ごとに別のトークンを参照している |
| 役割が同じ要素の書式の不統一 | エラー文言が、ある入力部品だけ書体が違う |
| レイアウトの `mode` | ラベル列が Figma は横並びなのに実装は縦積み |
| 束縛先とトークンの意味の食い違い | 角丸に余白のトークンが束縛されている |
| 既存コードとの差 | a11y 対応で Figma が寸法を変えたのにコードが追随していない |

#### 7-2. 既存の TODO をそのまま信じない

**実測し直し、成り立たなくなったものは落とす。** 後から入った上書きで解消済みなのに `TODO` だけが残っていることがある（コントラスト未達の `TODO` が全件解消済みだった例がある）。**残す TODO も、根拠を測り直してから残す。**

#### 7-3. 機械判定と合流させて 1 つの一覧にする

**検出項目は 3 カテゴリあり、一覧は `rules/common.md` §9 が正**（① デザインソースの不備 ② 設計の抜け ③ アーキテクチャ非適合）。
**3 カテゴリすべてを毎回通す。**「①だけ見て終わり」は診断漏れの典型なので、②③ を省略しない。
7-1 の所見と機械判定を**1 つの一覧にまとめ**、**Figma 側で直すもの**と**コード側で対処するもの**を分けて書く（操作者が「自分が動く分」を切り分けられるようにする）。ここではフロー上の扱いだけを定める:

- **チャットに直接提示する。** リポジトリに手順書ファイルを作らない（項目が多くて追いきれない場合のみ `output/diagnostics/<対象名>.md` に控えを置く。これは Push しない）。
- **カテゴリごとに小見出しを付けて出す**（①②③ のどこが問題だったかが分かる形にする）。該当ゼロのカテゴリは「①: 問題なし」の 1 行で済ませ、**該当しない項目を並べて長くしない**。全カテゴリ該当なしなら「診断: 問題なし」の 1 行。
- 診断後、**操作者に 3 択を提示して選ばせる**（Claude が決めない）:
  - **A. デザインソースを直してから再実行する** … 直すべき箇所の一覧を渡して、いったんフローを止める
  - **B. 今回はこの前提で進める** … 各問題について「どう扱うか」を明記し、生成コードに `TODO` コメントを入れ、PR 本文にも同じ一覧を残す
  - **C. 一部だけ直して残りは進める** … どれを直しどれを B 扱いにするかを一覧上で分ける。**実運用ではこれが一番使われる**ので、A/B の 2 択に丸めない
- **③ の数値照合は暗記でやらない。** `node scripts/effective-scale.mjs <作業ツリー> spacing:13px radius:20px …` で**実効スケールを実測**し、その出力を根拠にする（Tailwind の版とプロジェクトの `@theme` でスケールが変わるため。`rules/common.md` §9.3）。
- **① の命名も目視でやらない。** `components.json` があるなら `node scripts/lint-naming.mjs <components.json のパス>` で**部品名・プロパティ名・真偽値の接頭辞を機械判定**する（`rules/common.md` §9.1）。部品が増えると目視では必ず見落とす。綴り間違いだけは機械判定できないので目視で補う。
- **② のコントラストも目視でやらない。** `node scripts/contrast.mjs <作業ツリー> text-content-muted:bg-surface …` で**比を実測**し、その出力を根拠にする（`rules/common.md` §9.2）。**Disabled 状態は WCAG の例外なので未達として報告しない。** 背景が親から継承されている部品（表の見出し・補足・プレースホルダ等）は抽出から漏れるので、**置かれる面を仮定して必ず測る**。**ここで拾えば Figma 側の修正が生成前に済む** — a11y の CI でも同じ違反は落ちるが、そこまで行くと往復が 1 周増える。
- **数値のズレには必ず「寄せ先の候補」を添える**（例:「13px → `p-3`(12px) か `p-3.5`(14px)。どちらに寄せますか？」）。候補なしで「スケールに乗りません」だけ言わない。**候補はスクリプトの出力をそのまま使う**（暗記で書くと、実際には出ない値を提示することになる）。**Claude が勝手に丸めない。**

> 診断で問題が挙がったのに黙って B で進めるのは禁止。**A/B/C の選択は必ず操作者から取る。**

#### 7-4. A・C を選ばれたときの再開条件（**3 点が揃うまで再開しない**）

① Figma を直した ② **書き出し直した** ③ それを伝えた — の 3 点。
**②が抜けると、直したつもりで古いデータのまま進む。**

- **書き出しファイルのハッシュを取り込み前に照合する**（`shasum -a 256 <書き出しファイル>`。前回の値と比べる）。変わっていなければ**その事実を伝えて書き出しを依頼する**（「直したと聞いていますが、書き出しファイルが前回と同一です」）。**推測で進めない。**
- **再開したら 7-1 の下見からやり直す。** 前回の所見を再利用しない（直した結果、別の不整合が出ていることがある）。

#### 7-5. 生成中に新たな指摘が出たとき

- **軽微なもの**（命名のゆれ、1 か所だけの書式差など）は**貯めてステップ 10 の報告でまとめる**。都度止めると操作者の判断回数が増えるだけになる。
- **設計の前提が崩れるもの**（部品の分け方が変わる、状態が足りず実装できない等）だけ**即座に止めて相談する**。

### 8. コード生成（作業ツリーへ直接）

> **書き始める前に、該当ターゲットのルール冒頭のチェックリストを読む**（`rules/web-app.md` の「生成直前チェックリスト」）。
> ルールは長く生成中に全体を保持できないので、**間違えやすい点だけを凝縮した数行**が各ターゲットのルールの先頭にある。

生成の規約（詳細は各ターゲットのルール）:

- **デザイントークンはハードコードせず、ステップ 6 の逆引き表を通す**（原則 4）。表に当たらない値は書かず、ステップ 7 の診断へ戻す。
- `get_code` の出力・Claude Design の生成コード・`output/preview/` のモックは**いずれも流用しない**。構造の資料として読み、コードはルール準拠で書き直す。
- **ワークフローや設定ファイルの相対パスは、確認した配置先に合わせて置換してから書く**（`.github/workflows/` はリポジトリのルート直下に固定されるため、ここがずれやすい）。
- 移植先に既存ファイルがある場合（`vite.config` / `.storybook/` / `playwright.config.ts` / `eslint.config` / `routes.tsx` 等）は**無断上書きせず、差分をマージ提案として提示**する。

### 9. Storybook / テスト成果物の生成

生成物の「何ができるか」を明文化するため、**各ターゲットのルールに従って** Storybook とテスト・CI ワークフローを生成する。
**形式・配置・ワークフローの内容は `rules/web-app-storybook.md` / `rules/web-app-testing.md` / `rules/web-app-ci.md`（Web アプリ）/ `rules/web-lp.md`（LP は Storybook なし・E2E + VRT のみ）/ `rules/web-content-site.md` / `rules/native.md` が正。** ここではフロー上の約束だけを定める:

- **ストーリーはコンポーネント生成と同時に付ける**（`config` の `storybook`/`testing` で無効化されていない限り）。バリアント・状態を個別ストーリーとして列挙し、play 関数で動作を、a11y で使いやすさを検査対象にする。
- **VRT 対象の印（`tags: ['vrt']`）は付けない。** VRT への追加は共通層への昇格と同じく人間のプロセス。
- **画面を生成した場合のみ** E2E の受け入れ基準**雛形**を置く（本体は移植先で育成する前提）。画面が無ければ該当ワークフローごと作らない。
- **生成したテストを Push 前に走らせない**（原則 2）。壊れていれば PR のチェックが赤くなり、見た目のズレは公開された Storybook を人が見て気付く。そのための 3 層構成である。
- **足りない devDependencies・既存設定への差分は「報告するだけ」。** `package.json` や `.storybook/preview.ts` を勝手に書き換えず、インストールコマンドと差分を提示する。
  - **CI が「素通り」しないための前提は必ず伝える**: `.storybook/preview.ts` の a11y パラメータ（無いと a11y 違反でも緑）、`playwright.config.ts` / `playwright.vrt.config.ts` の `reporter: 'html'`（無いと差分画像が残らない）。
  - **CI で落とすかの基準は「実装起因は落とす / デザイン起因は落とさず報告する」**（`rules/web-app-testing.md` §6。デザイン起因を落とすと、Figma を直すまで無関係な PR まで全部赤くなって作業が止まる）。
  - **コントラストだけは例外で落とす**（`testing.a11yTest` = `error` / `testing.a11yReviewOnly` = 空）。**落としたら人に返すのがセット**（下記ステップ 11 の 6）。導入時に大量に出た場合の移行手順と、例外にしている理由は `rules/web-app-testing.md` §2。除外を足すときはデザイナーの合意と PR 本文への明記が必要。
- **雛形のブランチ名は `config` の `git.developBranch` / `git.mainBranch` に置換する**（運用が分からなければ確認する。develop を使わない運用なら main のみで回す）。既存ワークフローがあれば**上書きせずマージ提案**。
- ネイティブ / LP で対応する仕組みが無い場合は、生成を省略した旨を報告する。

#### 9-1. 実装者への案内（チャットと PR 本文だけで済ませる）

移植先の実装者は**エンジニアとは限らない**が、**そのための手順書ファイルをリポジトリに作らない**（`SETUP_GITHUB.md` のような設定手順書は生成しない。読み手は PR を見る人なので、置き場所は PR 本文とチャットが正しい）。

**Storybook を公開する構成のときだけ実施する**（LP ルートは Storybook を作らないので対象外。`storybook.publish` が `none` も同様）。
`storybook.pagesEnabled` が `false` の間だけ、以下を **PR 本文の冒頭に数行**＋**チャットで一言**伝える（詳細な文面は `rules/web-app-storybook.md` §3）:

1. **人手が必要な設定**: リポジトリ管理者が **Settings → Pages → Build and deployment → Source を「GitHub Actions」**に変更する。未設定だと公開ジョブだけが赤くなる。**Claude は実行できない。**
2. **公開範囲**: **「Private リポジトリだから安全」とは限らない**（Pro / Team では**サイトだけ公開される**）。**推測せず API で確認**してから伝える（判定表は `rules/web-app-storybook.md` §2）:
   `gh api repos/<owner>/<repo>/pages --jq '{public, html_url}'` → `public: true` = 誰でも閲覧可 / `false` = read 権限のあるサインイン済みのみ / 404 = Pages 未設定。
   誰でも閲覧可になる構成で未公開デザインを載せてよいか確認し、不可なら `storybook.publish` を `artifact` に切り替える。合意できたら `storybook.visibilityAcknowledged` を `true` にする。
3. **確認先**: 公開 URL と、公開タイミング（**開発ブランチにマージされた後**。PR 段階はテストのみ）。進行状況は Actions タブ。
   - **URL は決め打ちしない**（access control が有効だと `<org>.github.io/<repo>/` ではなく `<ランダム>.pages.github.io`）。上の `html_url` の実値を案内する。
4. **VRT が赤いとき**: 「見た目が変わった」の合図で不具合とは限らない。意図した変更なら Actions タブから「VRT の見本画像を更新」を手動実行する。

設定完了を確認できたら `config/project.local.json` の `storybook.pagesEnabled` を `true` に更新し、以後は再掲しない。

#### 9-2. Push 先を Claude Code で扱えるようにする（**これが無いと 2 回目から原則が全部外れる**）

受け取った人が Claude Code を起動するのは **Push 先のルート**。そこに `CLAUDE.md` もコマンドも無いと**原則 1〜4 が一切読み込まれない**（初回は Airis 側で作業するので気付けない）。

- Push 先のルートに **`CLAUDE.md` / `.claude/commands/` / `.claude/settings.json` を生成する**（既存があればマージ提案）。
- **生成物の中身・相対パスの読み替え・許可パターンの置換・`package.json` への検査コマンドは `rules/handoff.md` §1 が正。**

### 10. セルフチェック → デザイナーへ確認

#### 10-1. 生成物のセルフチェック（**確認より前に必ず通す**）

**生成の前には診断（7）があるのに、生成の後には何も無かった**ため、自分のルールに従えているかを誰も確認していなかった。ここが実装ミスの主な流入口なので、**静的検査を挟む**。

```bash
node scripts/selfcheck.mjs <作業ツリー> [--src <配置先>] --tsc
```

検査は 5 分類（**検査項目 22 個の一覧と根拠は `rules/web-app-selfcheck.md` §2 が正**）:

- **クラス名とトークンの整合** … `@theme` に無いクラス（**Tailwind は何も出力せずエラーも出さないので CI が一切捕まえられない**。★最大の穴）、`cn()` が独自の書体トークンに未対応で文字色が消える状態
- **構造の完全性** … ストーリー / E2E 雛形 / `tokens.css` / `sd.config.js` の欠落、ワークフローの参照パスずれ
- **ルール違反** … arbitrary value・生パレット・クラス文字列の三項演算子・`is` 接頭辞漏れ 等（`rules/web-app.md` §7「やらないこと」の機械検査）
- **ストーリーの中身** … `title` の欠落、バリアント未列挙、play 関数なし
- **型検査**（`--tsc`）… 型エラー・import 漏れ・存在しない props。**CI が赤くなる前に潰す**

- 既定では **今回生成/変更したファイルだけ**を検査する（移植先の既存コードの違反を拾わないため）。
- **ERROR は Push 前に直し、0 になるまで再実行する。** **WARN は勝手に直さず報告する**（`any` が避けられない、表示のみの部品で play が不要、等の判断は操作者のもの）。
- **型検査（`typescript` 未導入）とクラス名照合（`@theme` 未取得）は自動でスキップされる。** スキップされたら**必ず「未実施」と報告する**（黙って通ったことにしない）。どちらも対象リポジトリで `npm ci` すれば有効になる。

#### 10-2. デザイナーへ確認

- 生成したファイルの一覧と要点（コンポーネント名、対応するデザイン、Storybook ストーリー、ステップ 7 で保留にした項目）を簡潔に提示する。
- `git status` / `git diff` で差分を見せる（作業ツリーに直接書いているので、そのまま差分になる）。
- **ここでローカルのテスト・ビルド・Storybook 起動をしない**（原則 2）。見た目の確認は、マージ後に CI が公開する Storybook の URL で行う。
  - どうしても Push 前に見たいとデザイナーから求められた場合のみ、ローカル起動（`npm run storybook`）や `output/preview/` の HTML モックを**フォールバック**として案内する。
- 「**Claude Design で確認・手直ししたい**」と言われた場合: 生成物やモックを Claude Design にアップロードして編集してもらい、変更後のデザイン一式を受け取って再生成する（2-C パターン 2 / 3）。
- 「この結果を **Figma で手直ししたい**」と言われた場合（コード→Figma の還流）: 生成物やモックをブラウザで表示し、**HTML→Figma 取り込みプラグイン**（例: html.to.design）で読み込むと編集可能な Figma レイヤーになる。取り込みはデザイナーの操作であり、**Claude が Figma を直接書き換えることはできない**旨を正直に伝える（公式 API ではデザイン内容の書き込み不可）。

### 11. コミット → Push → PR 作成

**トークンも生成コードも同じブランチ・同じ PR にまとめる。** デザイナーに GitHub の画面を触らせない。

1. コミットメッセージを提案し、了承を得る。
2. `git add` → `git commit`（トークン `tokens/`・生成物 `styles/tokens.css` を含む。除外しない）。
3. **Push の直前に最終確認**を取る → `git push -u origin <branch>`。
   - **`.github/workflows/` を生成した回は、push の前に `gh auth status` で `workflow` スコープを確認する**（無いと確実に拒否される）。拒否されたときの切り分けは `rules/handoff.md` §2。
4. `gh pr create` で PR を作成する（マージ先はステップ 5 で確認した先）。PR 本文には:
   - 何を生成したか（コンポーネント / トークン / ストーリー / テスト雛形）
   - **ステップ 7 で保留にした項目**（Figma 側で直すべきこと。B を選んだ場合）
   - 足りない devDependencies・既存設定への差分提案（あれば）
   - **セルフチェック（10-1）で WARN のまま残した項目**（あれば）
   - **暫定で CI を落とさない設定にしている項目**（`testing.a11yReviewOnly` に規則を入れた場合）。「検査していない」のではなく「判断を人に返している」ことが分かるように書く
   - `storybook.pagesEnabled` が `false` の間は、ステップ 9-1 の 4 点を冒頭に数行で
5. PR の URL をデザイナーに伝える。マージ後の見た目の確認は公開 Storybook。
6. **CI の結果を Claude が確認し、デザイナーに日本語で翻訳して伝える**（下記 11-1）。
7. **マージされたら片付ける** — ブランチの削除は**内容を確認してから**（squash マージだと `--merged` も PR の表示も判定に使えない。手順は `rules/handoff.md` §3）。

- `tokens-studio` 方式の場合のみ、トークンは Tokens Studio の Push が別 PR として起票する。「トークンの PR が別に立っている」ことを伝えるだけでよい。
- **これ以外の方式で、デザイナーに GitHub の画面からファイルをアップロードさせない**（原則 1）。

> `git push` / `git remote` / `gh pr create` は確認を挟む設定です。実行前に必ずデザイナーの了承を得てください。

#### 11-1. CI が赤いときの扱い（**デザイナーに Actions を読ませない**）

**a11y やテストで CI を落とす設計は、「落ちたことがデザイナーに届く」経路が無ければ機能しない。**
デザイナーは Actions のログを読まない（原則 1）。**Claude が読んで翻訳するところまでがフローに含まれる。**

1. **結果を取る**: `gh pr checks <PR>` → 赤いジョブを `gh run view <run-id> --log-failed`。**落ちた層はジョブ名で分かる**（`component-test` = ①部品 / `story-test` = ②画面 / `visual-regression-test` = ③見た目）。
2. **原因を切り分ける**（**ここを飛ばして「Figma を直してください」と言わない**）:
   - **実装起因** = Claude の書いたコードの問題（トークンの当て間違い、`aria-label` の欠落、`cn()` の未対応で文字色が消える 等）。**Claude が直して同じブランチに追加コミットする。デザイナーには「直しました」だけ伝える。**
   - **デザイン起因** = トークンの値・組み合わせ自体の問題（コントラスト未達など）。**デザイナーに返す。**
   - 切り分けられないものは「調査中」として区別して出す（**推測で Figma のせいにしない**）。
3. **デザイン起因は「Figma で何をどう直すか」に翻訳する**（ステップ 7 の診断と同じ書式）:
   - **規則 ID を貼らない**。`color-contrast` → 「文字と背景の明るさの差が足りません」、`button-name` → 「ボタンの名前が読み上げられません」のように言い換える。
   - **どの Variable を、どの値に**（`scripts/contrast.mjs` の寄せ先候補）。**Claude が値を決めず、候補から選ばせる。**
4. **直った後**: Figma 修正 → トークンの取り込み（ステップ 6）→ 再生成（8）→ セルフチェック（10-1）→ **同じブランチに追加コミット**（PR は作り直さない）。

- **「PR を出したので終わり」にしない。** チェックが緑になるか、デザイナーに判断を返すまでが 1 サイクル。
- ③ VRT の赤は不具合ではない（意図した変更なら Actions の「VRT の見本画像を更新」を実行してもらう。ステップ 9-1）。

---

## エラー時の振る舞い

- MCP に繋がらない / 対象が特定できない / ルールが見つからない、等は**止まって理由と対処を日本語で説明**し、勝手に推測で進めない。
- **デザインソース側の不備は「エラー」ではなく「診断」として扱う**（ステップ 7）。黙って補完せず、直し方を提示して操作者に選ばせる。
- `gh` が未認証なら、対話ログインは Claude では完了できない。プロンプトに `! gh auth login` と入力してもらうよう案内する。
- 認証情報やトークンをログ・コミットに含めない（`.env` と `.claude/settings.local.json` は gitignore 済み）。
